/**
 * Placeholder Home. The real Home (Needs you / Addressed to you / Followed,
 * with no own-company identity header) is built in Milestone 8.
 */
export function HomePage() {
  return (
    <section className="flex flex-col gap-3">
      <h1 className="text-xl font-semibold text-ink">Ekum</h1>
      <p className="text-sm text-muted">
        Foundation is up. Home, Explore, Chats and Orders arrive as the features are built.
      </p>
    </section>
  );
}
